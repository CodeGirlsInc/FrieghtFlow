[package]
name = "reputation-tests"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["rlib"]

[dev-dependencies]
soroban-sdk = { version = "22.0.0", features = ["testutils", "alloc"] }
reputation = { path = "../../../reputation", features = ["testutils"] }
