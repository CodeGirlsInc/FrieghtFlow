[package]
name = "reward_distribution"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "21.5"

[dev-dependencies]
soroban-sdk = { version = "21.5", features = ["testutils"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
