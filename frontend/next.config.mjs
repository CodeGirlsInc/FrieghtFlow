/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['platform-lookaside.fbsbx.com', 's.gravatar.com', 'lh3.googleusercontent.com'],
  },
};

export default nextConfig;
